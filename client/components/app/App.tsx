import * as React from 'react';

require('./app.css');

export const App = () => (
    <React.Fragment>
        <div className="test">
            Test app2(client)
        </div>
    </React.Fragment>
);
